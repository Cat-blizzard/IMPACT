# 复现状态与问题日志

## 2026-09-17：二次审查修复（尚未重飞）

修复 P4 DESCEND 的遗留超时和仲裁授权状态字段，并将阶段 A 的事件存在检查替换为实际 rosbag 指令授权审计。证据不足保持 REVIEW_REQUIRED；不升级历史飞行验收。范围和验证边界见 [二次修复记录](CODE_REVIEW_FOLLOWUP_20260917.md)。

## 2026-09-17：代码审查修复（尚未重飞）

修复 P4/P16 的方向认证、时钟回退、飞控健康、失败终止和验收证据缺口，并新增对应回归。范围、测试和地图仍需内容核验的边界见 [代码审查修复记录](CODE_REVIEW_FIXES_20260917.md)。历史 CPU P4、GPU P4、旧 P5、阶段 A/B 和正式矩阵的飞行验收状态不因此升级。

## 2026-09-17：CPU P4 最终开发验收

冻结运行提交 `b88288f912797538dc0688b50a56fbc3c3fc668f` 已在 Mesa llvmpipe CPU 软件渲染下完成一次严格不解锁冒烟及三次独立冷启动 P4 正常任务；任务、定位、落地解除武装、bag/DataFlash 可读性和退出清理均通过。失败及中间运行全部保留，没有与最终组混算。完整指标、复现命令、哈希、尝试清单和限制见 [CPU P4 最终开发验收](CPU_P4_FINAL_VALIDATION_20260917.md) 与 [机器可读索引](../evidence/P4_CPU_20260917/validation-index.json)。

该结论仅覆盖 CPU SITL P4。GPU P4、旧 P5 FAIL、1.2 m `UNVERIFIED_FLIGHT`、阶段 A/B、正式矩阵和实机状态均未因此改变。

## 记录规则

本日志补充原仓库的历史阶段记录，不替换、删除或追改 `evidence/`、原阶段报告和原 README 状态表。
历史 PASS 仅表示对应版本、环境和门禁下的归档结果；当前复现必须单独记录。
后续进展以追加日期条目的方式保存，不把失败运行覆盖为成功。

证据分为：**本机代码/归档核对**、**服务器回传报告**、**待验证推断**。
服务器报告不等于本机已独立重放原始数据；构建和单元测试通过不等于完整飞行通过。

## 2026-09-16：首次复现审查快照

### 1. 版本与证据来源

- 原仓库基点：`6bcf0777975baddf2c08027c798731aefba549ed`。
- 本轮实现提交：`887f04b018f3cb93fb588fe91f56186bfa43aa52`；初次服务器目标含文档提交 `b4c1929e517c62f9732713c27d6b40951bbae894`。
- 路线文档提交：`1d1e4879dbc79bfefa2d2fe5cb8697bd3c74c097`。
- 服务器报告的本地修复：`7c280014ccb53d3896cccd6b54b936f8c98d452d`（里程计探针）、`a7d196352dc2f7897275831a6e1da135603dd55b`（MAVROS 超时与安全观察点）。
- 服务器随后报告已备份、保留 stash 并合并路线文档；新的合并提交号尚未回传。
- 写入本日志前抓取远端，`origin/main` 仍为 `1d1e487`。本机尚未取得服务器两个修复提交和完整诊断包，不能声明已完成源码审查或两端同步。本日志提交后远端文档版本会更新。

历史资料：[P5 汇总](../evidence/P5/summary.json)、[P5 任务记录](../evidence/P5/mission-result.json)、[bag 元数据](../evidence/P5/rosbag-metadata.yaml)、[大型数据哈希清单](../evidence/P5/LARGE_ARTIFACTS.sha256)、[阶段归档索引](PHASE_ARCHIVE_INDEX.md)。
历史原始 bag 未包含在上述轻量归档中，本轮未独立重放该完整历史包。

### 2. 当前阶段表

| 阶段 | 历史归档状态 | 当前复现结论 |
|---|---|---|
| P0—P3 | 原仓库记录 PASS | 服务器报告环境检查、构建和真实传感器/FAST-LIO 输出；逐阶段完整门禁尚需核对 |
| P4 | 原仓库记录 PASS | 共享起飞链路存在异常；独立完整 P4 待重验，不能以 P5 的起飞失败替造 P4 验收记录 |
| P5 | 原仓库记录 PASS | 当前试跑 FAIL；探索停滞和起飞失败分别记录 |
| P6—P15 | 原仓库记录 PASS | 当前服务器未完成各阶段复验，不能标记当前通过，也不能据 P5 失败全部判失败 |
| COMPLEX | 原仓库记录 PASS | 当前服务器未复验 |
| P16 | 本机测试已有通过记录 | 完整服务器闭环未验收；正式矩阵和最终视频未完成 |
| HW | TODO | 本轮不开展实机验证 |

本机曾完成 8 个相关 ROS 包构建、160 项测试和真实 DDS/TF 接口检查；没有完成本机 Gazebo 飞行闭环。
服务器最新回传为 14 个包构建通过、165 项测试通过、ROS 合约和 GPU doctor 通过。这些检查不能替代 mission PASS。

### 3. 原仓库实现问题与待查现象

| 编号 | 发现及证据性质 | 当前状态与结论边界 |
|---|---|---|
| R01 就绪探针 | 旧脚本使用 `ros2 topic echo --once`，超时便报告 FAST-LIO 未发布；服务器同轮 bag 报告含 1021 条 odom | 服务器已用真实订阅探针替换，并报告约 4.1–4.2 秒收到首帧；支持旧诊断误判，不能把 CLI 超时等同于无数据 |
| R02 连续导航健康 | 本机核对原 `_nav_ready()`：检查收到过 odom、适配器状态新鲜且健康、存在订阅者和参数验证；未建立连续 FCU/EKF 健康窗口 | 实现缺少持续健康证据；补门禁不等于已经修复定位异常根因 |
| R03 探索推进 | 原 Frontier 优先选择可达前沿格，缺少候选时才尝试偏移视点；服务器报告首目标到达后地图更新，膨胀后可达自由单元为 1，仍有 9 个前沿簇 | 原选点及受阻处理需要完善；真实障碍、假占用、时间关联、净空参数的作用尚待拆分。EGO 碰撞地图未完整补录，不能证明异常只在 Frontier |
| R04 失败终止 | 本机核对原 P5：部分任务超时、探索超时等分支直接 `_finish(FAIL, ...)`，没有进入正常 LAND/DESCEND 确认流程 | 明确的失败路径缺口；任务结果、终止请求和落地解除武装确认必须分开记录 |
| R05 起飞异常 | 服务器两轮报告 `VisOdom: not healthy`、EKF variance、切 LAND、Potential Thrust Loss；另有 roll/pitch diff 13.5 deg 信息 | 根因未确定。需关联原始定位、适配器输出、FCU 状态与飞控日志；不能直接认定某个转换错误，也不能归因于尚未执行的观察点规则 |
| R06 完成判据 | 历史 PASS 的可达自由单元为 1、前沿簇为 0；原代码从安全膨胀过滤后的自由区构造前沿 | 应区分探索完成与过滤导致前沿消失。单凭 reachable=1 不能判定历史误报，也不能证明当前收缩原因 |
| R07 重复性证据 | 已检查到历史成功记录与轻量证据，但没有掌握完整重复试验次数、失败率和跨环境对照 | 不能据此声称原作者未验证，也不能将特定运行 PASS 扩大为稳定跨平台保证 |

相关实现：[原有 P4 任务节点](../src/xq_autonomy/xq_autonomy/p4_mission_node.py)、[原有 P5 任务节点](../src/xq_autonomy/xq_autonomy/p5_mission_node.py)、[Frontier 节点](../src/xq_autonomy/xq_autonomy/p5_frontier_node.py)、[P5 启动脚本](../scripts/run_p5_baseline.sh)。链接指向随仓库演进的代码；本条目的历史判断以以上版本号为准。

### 4. 历史成功与当前失败如何对照

历史 P5 汇总记录 PASS、4 个目标（2 到达、2 失败）、110 条 B-spline、约 43.17 m 路径、零碰撞样本、最终 LAND 且解除武装。
任务事件还记录了 7 次 ARM 拒绝后第 8 次接受；没有对应完整飞控日志，不能认定拒绝与当前 EKF 异常同源。
探索允许放弃目标，故“2 个目标失败”本身不证明验收不严谨；应检查最终任务完成条件是否充分。

| 项目 | 历史成功记录 | 当前探索失败报告 |
|---|---|---|
| 可达自由单元 | 1 | 1 |
| 前沿簇 | 0 | 9 |
| 下一观察点 | 无 | 无 |
| finished | true | false |
| 任务结果 | PASS | FAIL |

本机比较 `6bcf077..b4c1929`，P4 任务、P5 任务及 P5 Frontier 三个文件无改动，说明上述沿用逻辑并非新完整性机制引入。
但本轮修改了安装/启动方式与渲染配置（原脚本强制软件渲染，服务器配置使用 GPU）；依赖和时序影响尚未排除。
当前证据既不支持“原仓库从未验证”，也不支持“所有故障都与本轮改动无关”。

### 5. 服务器运行证据索引（回传信息，非已上传附件）

以下路径相对服务器仓库根目录；完整包尚未随本日志提交：

| 运行 | 已报告结果 |
|---|---|
| `experiments/results/baseline_v1/p5_server_31c9ad23/` | CLI odom 等待超时；bag 有 1021 条 odom，mission 未启动 |
| `experiments/results/baseline_v1/p5_server_df0053d5/` | 首目标到达；1 个目标、33 条 B-spline、约 30029 条 position_cmd；9 个前沿簇、293 个前沿单元、1 个可达单元，任务未完成 |
| `experiments/results/baseline_v1/p5_server_20c2e7c0/` | 起飞、EGO 和首目标执行成功，后续无可达观察点；mission FAIL，安全评估单独 PASS |
| `experiments/results/baseline_v1/p5_server_cf1fcc95/` | 更严格观察点配置下，在起飞阶段失败，未验证观察点规则 |
| `experiments/results/baseline_v1/p5_server_258ac18f/` | 再次起飞失败，未验证观察点规则 |

服务器报告已保留对应 `<运行目录名>-diagnostics.tar.gz`。后续接收时应追加完整提交号、配置哈希、日志校验值和独立复核结论。
约 3 万条 position_cmd 不等于 3 万次规划或持续推进；安全评估 PASS 不等于任务完成；切 LAND 不等于已落地解除武装。

### 6. 修复和后续验收边界

- 服务器已报告探针、MAVROS 超时/重试和安全观察点改进；本机尚未取得对应修复源码。
- 1.2 m 已知占用净空过滤有单元测试，后续飞行在起飞阶段失败；维持 **UNVERIFIED_FLIGHT**，不能宣布解决探索停滞。
- 下一步：定位/飞控稳定性 → 共享地图排查 → 阶段 A 基础目标导航 → 阶段 B 完整性与恢复 → 阶段 C 自主探索回接。详见 [开发路线](DEVELOPMENT_ROUTE.md)。
- 固定配置的多次独立冷启动用于开发检查；少量连续成功不构成可靠性统计证明。姿态和时间诊断须结合原始传感器时间与接收时间，持续监测起飞后的健康状态。
- 共享地图离线分析、阶段 A 代码可以同步推进。阶段 A 需要独立协议、实际到达与失败终止，不改名复用旧 P5 PASS；当前文档新增不代表验收入口已经实现。
- CPU 开发测试和 GPU 完整仿真分开验收。CPU 完整飞行未验证时不声称支持；GPU 一轮成功也不扩大为跨环境保证。
- 不放宽安全参数、关闭故障保护或将超时降落改记任务成功来获得 PASS。正式 120 次矩阵仍待前置验收完成。

后续每条日志至少追加：日期、代码提交、环境/配置、协议和运行 ID、任务结果、终止结果、证据位置、确认或排除的原因，以及仍待验证事项。
